# ============================================================================
# Docker Compose Secrets Template
# ============================================================================
# 1. Copy this file to docker/.env
# 2. Replace placeholder values
# 3. Never commit docker/.env to git!
# ============================================================================
# Keep ONLY sensitive values here
# Non-sensitive defaults are in docker-compose.yml
# ============================================================================


# ============================================================================
# PostgreSQL Database Secrets
# ============================================================================
# Credentials for PostgreSQL service (used by n8n)
# IMPORTANT: Change these to secure values!
# ============================================================================

POSTGRES_DB=research_assistant
POSTGRES_USER=research_assistant
POSTGRES_PASSWORD=research_password
# Database password
# IMPORTANT: Generate a strong password!
# Example: openssl rand -base64 32
# Must match POSTGRES_PASSWORD in application .env


# ============================================================================
# n8n Workflow Automation Secrets
# ============================================================================
# n8n service configuration
# ============================================================================

N8N_ENCRYPTION_KEY=your_encryption_key_here
# Encryption key for n8n credentials storage
# IMPORTANT: Generate a secure random key!
# Example: openssl rand -hex 24
# NEVER change this after first use (will break existing credentials)


# ============================================================================
# End of Configuration
# ============================================================================