# ===== ResearchAssistantGPT | Docker Environment Template =====
# docker/.env  (used by docker/docker-compose.yml for ${VAR} substitution)
# Copy this file to ".env" and fill values as needed.
# Never commit your .env file.

N8N_PORT=5678
N8N_ENCRYPTION_KEY=changeme-super-secret-key

POSTGRES_DB=n8n_db
POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=n8n_password
POSTGRES_PORT=5432